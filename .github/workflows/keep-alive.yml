name: Keep Services Alive

# 毎日実行 + 手動実行も可能
on:
  schedule:
    # 毎日午前3時（UTC）に実行 = 日本時間 正午
    - cron: '0 3 * * *'
  workflow_dispatch: # 手動実行を許可

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Keep Supabase Active
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: |
          echo "📡 Supabaseにクエリを送信してアクティブ状態を維持..."

          # 環境変数のチェック
          if [ -z "${SUPABASE_URL}" ]; then
            echo "❌ エラー: SUPABASE_URLが設定されていません"
            echo "GitHub Settings > Secrets and variables > Actions で設定してください"
            exit 1
          fi

          if [ -z "${SUPABASE_ANON_KEY}" ]; then
            echo "❌ エラー: SUPABASE_ANON_KEYが設定されていません"
            echo "GitHub Settings > Secrets and variables > Actions で設定してください"
            exit 1
          fi

          echo "🔍 Supabase URL: ${SUPABASE_URL}"
          echo "🔑 Anon Key: ${SUPABASE_ANON_KEY:0:20}... (マスク済み)"

          # 複数のテーブルに実際のデータ取得クエリを送信
          echo "📤 クエリ1: profilesテーブル（実データ取得）..."
          response1=$(curl -s -w "\n%{http_code}" \
            -H "apikey: ${SUPABASE_ANON_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_ANON_KEY}" \
            "${SUPABASE_URL}/rest/v1/profiles?select=id,username&limit=5" || echo "CURL_FAILED")

          if [ "$response1" = "CURL_FAILED" ]; then
            echo "❌ profilesクエリ失敗"
            exit 1
          fi

          http_code1=$(echo "$response1" | tail -n1)
          echo "📥 profiles HTTPステータス: $http_code1"

          if [ "$http_code1" -ne 200 ] && [ "$http_code1" -ne 206 ]; then
            echo "⚠️ profilesクエリ失敗 (HTTP $http_code1)"
            exit 1
          fi
          echo "✅ profilesクエリ成功"

          # gamesテーブルにもクエリ
          echo "📤 クエリ2: gamesテーブル（実データ取得）..."
          response2=$(curl -s -w "\n%{http_code}" \
            -H "apikey: ${SUPABASE_ANON_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_ANON_KEY}" \
            "${SUPABASE_URL}/rest/v1/games?select=id,name&limit=5" || echo "CURL_FAILED")

          if [ "$response2" != "CURL_FAILED" ]; then
            http_code2=$(echo "$response2" | tail -n1)
            echo "📥 games HTTPステータス: $http_code2"
            if [ "$http_code2" -eq 200 ] || [ "$http_code2" -eq 206 ]; then
              echo "✅ gamesクエリ成功"
            fi
          fi

          # auth_user_scoresテーブルにもクエリ
          echo "📤 クエリ3: auth_user_scoresテーブル（実データ取得）..."
          response3=$(curl -s -w "\n%{http_code}" \
            -H "apikey: ${SUPABASE_ANON_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_ANON_KEY}" \
            "${SUPABASE_URL}/rest/v1/auth_user_scores?select=id,score&limit=5" || echo "CURL_FAILED")

          if [ "$response3" != "CURL_FAILED" ]; then
            http_code3=$(echo "$response3" | tail -n1)
            echo "📥 auth_user_scores HTTPステータス: $http_code3"
            if [ "$http_code3" -eq 200 ] || [ "$http_code3" -eq 206 ]; then
              echo "✅ auth_user_scoresクエリ成功"
            fi
          fi

          echo "🎯 複数テーブルへのアクティビティ完了"

      - name: Keep Vercel Deployment Active
        env:
          VERCEL_URL: ${{ secrets.VERCEL_URL }}
        run: |
          echo "🌐 Vercelデプロイメントにアクセス..."

          # Vercel URLが設定されている場合のみアクセス
          if [ -n "${VERCEL_URL}" ]; then
            response=$(curl -s -w "\n%{http_code}" -L "${VERCEL_URL}")
            http_code=$(echo "$response" | tail -n1)

            if [ "$http_code" -eq 200 ]; then
              echo "✅ Vercelアクセス成功 (HTTP $http_code)"
            else
              echo "⚠️ Vercelアクセス失敗 (HTTP $http_code)"
              # Vercelの失敗は致命的エラーとしない（オプショナル）
            fi
          else
            echo "ℹ️ VERCEL_URLが設定されていないためスキップ"
          fi

      - name: Summary
        run: |
          echo "🎉 Keep-Aliveジョブ完了"
          echo "次回実行: 24時間後（毎日）"
